model:
  model_name_or_path: "gemini-3.1-flash-lite"
  model_type: "gemini-3.1-flash-lite"
inference:
  shot: 0  # 0 for 0-shot, 1 for 1-shot
  delay: 1.0  # delay between API calls (seconds)
dataset:
  data_path: "src/datasets/data/test_500.jsonl"
  image_dir: "data/images/COCO2017"
output:
  output_dir: "outputs/gemini"
